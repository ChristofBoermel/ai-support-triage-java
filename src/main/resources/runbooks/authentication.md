# Authentication runbook

- Determine whether the failure affects one user, one tenant, or all users.
- Check identity-provider status, certificate validity, and recent authentication configuration changes.
- Review authentication logs for failed login, token, session, or permission errors.
- Verify recent changes to secrets, redirect URLs, callback configuration, or role mappings.
- Confirm that system clocks and required identity-provider network connectivity are healthy.